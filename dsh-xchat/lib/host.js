// ============================================================================
// 跨会话知识桥 dsh-xchat — Host 半边（静态版，no-op）
// ----------------------------------------------------------------------------
// xchat_query 工具已迁至 agent preset 层（见本包 preset/ 目录，预设
// 「XChat 创造模式」）。这里保留空 apply，仅为让 profile 的
// cordis.patch.yml 挂载行无害通过（client 半负责 @ 会话菜单与拖拽）。
// ============================================================================
export default {
  name: 'dsh-xchat',
  apply() {
    // no-op
  },
}
